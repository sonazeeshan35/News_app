import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'News_app-api',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
